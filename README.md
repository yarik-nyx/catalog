# Catalog

```markdown
## Установка

1. Клонируйте репозиторий: `git clone https://github.com/yarik-nyx/catalog.git`
2. Установите зависимости: `poetry install --no-root`
3. Создайте файл .env, внесите параметр: `DB_URL`
3. Запустите проект: `python poetry run python src/main.py`
```
