# Catalog

```markdown
## Установка

1. Клонируйте репозиторий: `git clone https://github.com/yarik-nyx/catalog.git`
2. Установите зависимости: `pip install -r reqs.txt`
3. Создайте файл .env, внесите 2 параметра: `DB_URL` и `SECRET_KEY`
3. Запустите проект: `python src/main.py`
```
