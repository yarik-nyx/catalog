# Catalog

```markdown
## Установка

1. Клонируйте репозиторий: `git clone https://github.com/yarik-nyx/catalog.git`
2. Установите зависимости: `poetry install --no-root`
3. Создайте файл .env, внесите 2 параметра: `DB_URL` и `ALLOWED_IPS`
3. Запустите проект: `poetry run python src/main.py`
```
